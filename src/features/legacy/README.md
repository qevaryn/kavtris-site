# Legacy feature components

This folder keeps components retained from earlier homepage structures.

They are not part of the current public route composition after the website simplification phases. They are kept temporarily so product, design and engineering can review whether any concept should be reused before deletion.

Deletion requires a separate cleanup review and regression validation.
