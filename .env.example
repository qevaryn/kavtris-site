# Required for real Resend delivery outside local mock mode.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
RESEND_TO_EMAIL=

# Public canonical site URL used by metadata and links.
NEXT_PUBLIC_SITE_URL=

# Use true only for local development/test environments that must not send real email.
CONTACT_FORM_MOCK=false
